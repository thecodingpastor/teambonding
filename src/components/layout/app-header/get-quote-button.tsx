import { Button } from "@/components/ui/button";

const GetQuoteButton = () => {
  return (
    <Button hideArrow className="uppercase font-bold">
      Get a quote
    </Button>
  );
};

export default GetQuoteButton;
